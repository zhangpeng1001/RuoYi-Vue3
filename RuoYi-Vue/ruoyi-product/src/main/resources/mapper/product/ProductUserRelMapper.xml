<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.product.mapper.ProductUserRelMapper">

    <!-- 关联 sys_user / sys_dept 装配用户信息 -->
    <resultMap type="ProductUserRel" id="ProductUserRelResult">
        <result property="id"         column="id"          />
        <result property="productId"  column="product_id"  />
        <result property="userId"     column="user_id"     />
        <result property="createTime" column="create_time" />
        <result property="userName"   column="user_name"   />
        <result property="nickName"   column="nick_name"   />
        <result property="deptName"   column="dept_name"   />
    </resultMap>

    <!-- 按商品ID查询所属人（关联 sys_user + sys_dept） -->
    <select id="selectUsersByProductId" parameterType="Long" resultMap="ProductUserRelResult">
        select r.id, r.product_id, r.user_id, r.create_time,
               u.user_name, u.nick_name, d.dept_name
        from product_user_rel r
        left join sys_user u on r.user_id = u.user_id
        left join sys_dept d on u.dept_id = d.dept_id
        where r.product_id = #{productId}
        order by r.id asc
    </select>

    <!-- 批量新增关联 -->
    <insert id="batchInsertRel">
        insert into product_user_rel (product_id, user_id, create_time) values
        <foreach collection="list" item="item" separator=",">
            (#{item.productId}, #{item.userId}, #{item.createTime})
        </foreach>
    </insert>

    <!-- 按商品ID物理删除关联（关联表无 is_deleted 字段） -->
    <delete id="deleteByProductId" parameterType="Long">
        delete from product_user_rel where product_id = #{productId}
    </delete>
</mapper>
