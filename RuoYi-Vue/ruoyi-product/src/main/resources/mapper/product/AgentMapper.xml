<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.product.mapper.AgentMapper">

    <resultMap type="Agent" id="AgentResult">
        <result property="id"         column="id"          />
        <result property="agentName"  column="agent_name"  />
        <result property="contact"    column="contact"     />
        <result property="phone"     column="phone"        />
        <result property="status"     column="status"      />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="isDeleted" column="is_deleted"   />
    </resultMap>

    <!-- 按关键字模糊查询启用且未删除的代理商 -->
    <select id="selectAgentList" parameterType="Agent" resultMap="AgentResult">
        select id, agent_name, contact, phone, status, create_time, update_time, is_deleted
        from agent
        <where>
            <if test="isDeleted != null"> and is_deleted = #{isDeleted}</if>
            <if test="isDeleted == null"> and is_deleted = 0</if>
            <if test="status != null"> and status = #{status}</if>
            <if test="agentName != null and agentName != ''"> and agent_name like concat('%', #{agentName}, '%')</if>
        </where>
        order by id desc
        limit 20
    </select>
</mapper>
