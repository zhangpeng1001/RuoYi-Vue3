<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.product.mapper.SysRegionMapper">

    <resultMap type="SysRegion" id="SysRegionResult">
        <result property="id"          column="id"           />
        <result property="regionCode" column="region_code"   />
        <result property="regionName" column="region_name"   />
        <result property="parentCode" column="parent_code"   />
        <result property="level"      column="level"         />
        <result property="sortOrder"  column="sort_order"    />
    </resultMap>

    <!-- 查询所有省份（level=1） -->
    <select id="selectProvinces" resultMap="SysRegionResult">
        select id, region_code, region_name, parent_code, level, sort_order
        from sys_region
        where level = 1
        order by sort_order asc, id asc
    </select>

    <!-- 根据省编码查询城市列表（level=2） -->
    <select id="selectCitiesByParentCode" parameterType="String" resultMap="SysRegionResult">
        select id, region_code, region_name, parent_code, level, sort_order
        from sys_region
        where level = 2 and parent_code = #{parentCode}
        order by sort_order asc, id asc
    </select>
</mapper>
